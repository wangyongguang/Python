###切片
* 取list或tuple**部分元素**
    1. L = ['1','2',...'15']    
    取前3个元素L[0:3],其中0表示开始位置，3表示直到索引3为止
    2. 或取倒数第一个为L[-1]
    3. 后10个数L[-10:]
    4. 前11-20个数L[10:20]
    5. 前10个数，每两个取一个为：L[0:10:2]
    6. 所有数，每5个取一个为：L[::5]
    